<div class="card card-custom text-color-custom p-5">
    <div class="card-body">
        <h1 class="title-post-custom">{{post.titulo}}</h1>
        <p class="mt-5 post-content-custom">{{post.content}}</p>
        <hr class="my-4">
        <div class="d-flex-custom justify-content-between">
            <small class="small-custom">Data de publicação: {{post.date}}</small><br>
            <small class="small-custom">Autor(a): {{post.author}}</small>
        </div>
        <hr class="my-4">
    </div>
</div>